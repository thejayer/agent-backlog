{
  "name": "agent-backlog",
  "version": "0.1.0",
  "private": false,
  "description": "A task board your coding agents claim work from. Backlog packets render as agent prompts; agents claim with a lease and write status back.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/thejayer/agent-backlog.git"
  },
  "homepage": "https://github.com/thejayer/agent-backlog#readme",
  "bugs": {
    "url": "https://github.com/thejayer/agent-backlog/issues"
  },
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vite --config manage/vite.config.mjs --host 127.0.0.1",
    "build": "vite build --config manage/vite.config.mjs",
    "start": "node manage/server.mjs",
    "preview": "vite preview --config manage/vite.config.mjs --host 127.0.0.1",
    "test": "playwright test --config manage/playwright.config.mjs --grep-invert @a11y",
    "test:a11y": "playwright test --config manage/playwright.config.mjs --grep @a11y",
    "test:unit": "vitest run --config manage/vitest.config.mjs",
    "smoke": "node scripts/smoke.mjs",
    "agent": "node scripts/manage-agent.mjs",
    "manage:dev": "vite --config manage/vite.config.mjs --host 127.0.0.1",
    "manage:build": "vite build --config manage/vite.config.mjs",
    "manage:serve": "node manage/server.mjs",
    "manage:preview": "vite preview --config manage/vite.config.mjs --host 127.0.0.1",
    "manage:test": "playwright test --config manage/playwright.config.mjs --grep-invert @a11y",
    "manage:test:a11y": "playwright test --config manage/playwright.config.mjs --grep @a11y",
    "manage:agent": "node scripts/manage-agent.mjs"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.12.1",
    "@playwright/test": "^1.60.0",
    "@vitejs/plugin-react": "^4.3.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "vite": "^6.0.0",
    "vitest": "^3.2.4"
  },
  "optionalDependencies": {
    "@google-cloud/firestore": "^8.6.0"
  }
}
